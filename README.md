```Simple TODO app made with FastAPI```

## Installation

1) Clone the repository
2) Create a virtual environment and install dependencies from pipfile or requirements.txt
3) set environment variables DATABASE_URL and SECRET_KEY
4) Start server with run uvicorn main:app --reload
